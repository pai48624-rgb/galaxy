# 로컬 테스트 → 배포 (혼자 할 때)

새 Claude 세션 없이도 이 두 명령어만 터미널에 그대로 치면 됩니다.
(`cd C:\dev\galaxy` 상태에서 실행)

---

## 1. 로컬에서 테스트

```
npm test
```

- 자동으로 최신 `index.html` 등을 `dist/`에 복사(빌드)한 다음, 로컬 서버를 띄웁니다.
- 터미널에 `Ready on http://127.0.0.1:8788` 이 뜨면 성공 → 브라우저로 그 주소 열어서 확인.
- 회원가입/로그인처럼 서버(API) 기능도 실제 운영 환경과 똑같이 동작합니다(운영 DB에 실제로 연결됨 — 테스트 계정으로 확인하세요).
- 끝내려면 터미널에서 `Ctrl+C`.

## 2. 문제 없으면 배포

```
npm run deploy
```

- 마찬가지로 자동 빌드 후 Cloudflare Pages(`aigalaxy-map.com`)에 올립니다.
- 끝나면 터미널에 `https://aigalaxy-map.com` 링크가 표시됩니다. 그 주소로 확인하세요.

---

## 알아두면 좋은 것

- **파일 고치는 곳은 항상 루트**입니다 (`index.html`, `privacy.html` 등). `dist/` 폴더는 절대 직접 수정하지 마세요 — `npm test`나 `npm run deploy`를 실행할 때마다 루트 파일로 자동으로 덮어써집니다.
- 배포 전에 새 파일(`data/`에 새 json 추가 등)을 추가했다면, `scripts/build-dist.mjs` 안의 목록에도 파일명을 추가해야 배포에 포함됩니다. (모르겠으면 다음 Claude 세션에서 "새 파일 X를 배포에 포함시켜줘"라고 말씀하시면 됩니다.)
- 로그인이 만료돼서 배포가 안 되면(`wrangler` 인증 오류) `npx wrangler login` 실행 → 브라우저에서 로그인.
