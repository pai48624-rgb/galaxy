# 작업 요약 (2026-09-12)

## 0. 추가 라운드 — "함께 쓰면 좋은 AI"가 엉뚱하다는 피드백 원인과 조치

**원인을 실제로 찾아서 검증함**: 사이트에 원래 있던(이번 세션 이전부터 있던) `relations` 데이터
2184개를 전수 조사한 결과, **전부 `alternative`(경쟁·대안) 관계였고 `complement`(보완재/함께 쓰기 좋음)
관계는 단 한 건도 없었습니다.** 그래서 어떤 AI를 클릭하든 "함께 쓰면 좋은 · 비슷한 AI"라는 이름으로
사실은 "경쟁 제품 목록"만 보여주고 있었던 것 — 사용자가 지적한 "엉뚱하다"는 느낌의 실제 원인이었습니다.

조치:
- 라벨을 정직하게 분리: "대안 AI(비슷한 경쟁 제품)"은 따로 접어서(collapsed) 낮은 우선순위로 표시.
- Top10(가장 많이 쓰이는 AI) 10개는 웹서치로 실제 연동 관계를 새로 조사해
  `data/ai_top10_relations.json`에 저장(MCP 서버, 공식 파트너십, 플러그인 등 구체적 연동 방식 명시,
  사용자가 예시로 준 "힉스필드 연관 AI" 수준의 구체성 목표). 상세패널에 "🔗 실제 연동·연관 AI 툴"
  섹션으로 표시.
- 나머지 721개 툴은 전수 재조사가 이번 세션에 물리적으로 불가능해서(2184건 개별 웹검증은 비현실적),
  참조문서 기반 `ai_combos.json`(686개, 91% 매칭률 확인됨)을 "함께 쓰면 좋은" 1순위 근거로 승격.
- 오른쪽 상세패널이 너무 길다는 지적도 반영: 핵심 기능/가격 상세/대안/리뷰를 접이식(details)으로
  바꾸고, 맨 위에 "가장 잘 맞는 조합 + 별점"을 한 줄 요약으로 추가.

**정직한 한계**: `description`(한 줄 설명) 자체는 이번에 다시 쓰지 않았습니다 — 확인해보니 내용 자체는
틀리지 않았고(Claude/Gemini 등 표본 확인), 다만 한 줄로 짧습니다. 731개 전체를 다시 조사해서 서술을
풍부하게 만드는 건 이번 라운드 범위 밖으로 남겨둡니다.

## 1. 참조 문서 3개를 어떻게 반영했는가

- `AI_가격비교_마케팅조합_매트릭스.docx` (26개 2단계 조합)
- `AI_가격비교_마케팅조합_매트릭스_2부.docx` (360개, 8개 뇌모델 × 45개 실행툴)
- `AI_멀티툴_3to5단계_조합_매트릭스.docx` (300개, 10개 뇌모델 × 30개 운영패턴)

세 문서 모두 텍스트로 전량 추출해 재확인한 결과 **중간에 끊긴 곳 없이 끝까지 완전한 문서**였고,
번호(1~26 / 1~360 / 1~300)와 실제 파싱된 항목 수가 정확히 일치했습니다(686개 = 26+360+300,
`data/ai_combos.json`). 사용자가 걱정한 "복사 누락"은 없었습니다.

각 조합은 `{tools, brain, executors, title_ko(마케팅 활용), impact(★ 개수), workflow_ko(데이터/알고리즘
연동 방식), pricing_summary_ko(요금제), category}`로 구조화했고, 웹서치 기반 AI 툴 사용순위
Top10(`data/ai_top_rank.json`)도 함께 만들었습니다.

**이 데이터가 실제로 화면에 어떻게 쓰이는지**(요청하신 "결과에 표시"):
- 개별 AI 상세패널(별 클릭) → 그 AI가 포함된 조합 카드(파급력 별점 + 활용처 + 요금) 최대 4개
- **카테고리(별자리) 선택 시** → 오른쪽 상세패널에 그 카테고리 AI 전체를 대상으로 한 **종합평가 문장**
  (무료/부분유료 비율, 가장 파급력 큰 조합, 무료로 시작 가능한 조합 수를 실시간 집계해서 문장으로 합성)
  + 관련 조합 카드 최대 8개 — `synthesizeCategoryEval()`이 매번 그 카테고리 데이터를 계산해서 씀
  (미리 써둔 고정 문구 아님)
- 사이드바 상단 "⭐ 추천 AI 조합 TOP" — 전체 조합 중 파급력 순 30개

## 2. 이번 세션에 추가한 기능 전체 목록

- 은하 배경(NASA/ESA/ESO 실사진 22종: 은하10·성운6·성단2·블랙홀2·초은하단2), 노출시간 기존 대비 2배
- 태양계(태양+8행성, 실사진 텍스처, 토성·천왕성 고리, 공전·자전 애니메이션) — 빈 공간(-X쪽)에 배치
- 88개 IAU 별자리(실좌표, d3-celestial 데이터 기반) — 카테고리에 실제 별자리 이름 매칭, 미사용
  ~57개는 지도 맨 바깥에 장식으로 표시
- AI 사용순위 Top10 — 다른 별보다 크게, 고리 장식(토성형/천왕성형/해왕성형/골드형 4종)
- 제휴 "태양"(steellotto.duckdns.org → ☀ 포르투나(로또추첨)) — 클릭 시 제휴링크 이동,
  `data/affiliate_suns.json`에 항목 추가하면 자동으로 늘어남
- 오디오 볼륨 3배 증폭 + 컴프레서(클리핑 방지)
- 하단 아이콘 툴팁 전체 추가
- 사이드바 재배치: 메뉴 버튼(조합TOP·AI추천)을 카테고리보다 위로, 카테고리는 맨 아래로
- 상단 안내 배너: 3D 공간에서 별 확대가 안 될 때 "드래그로 전체를 돌려보라"는 안내(닫으면 기억함)
- 별 이름표: 클릭 안 해도 보이도록 기본적으로 항상 표시(거리에 따라 흐려지되 완전히 사라지진 않음)
- 지도 형태에 "지구형" 추가(카테고리별로 지구 표면 위 한 지점에 배정해 별자리처럼 배치) — 실험적 기능
- 세계 도시 지구본(나라→수도→주요도시, 실제 위경도, 클릭 시 인구·핵심정보 토스트) —
  `data/world_countries.json` (기존 AI 데이터와 무관한 별도 배경 콘텐츠)

## 3. 확인 못 한 부분 (정직하게 남김)

- 이 세션에서 브라우저 확장(Claude in Chrome)이 꺼져 있어 실제 렌더링을 직접 눈으로 보지 못했습니다.
  문법 검사(node --check)와 리소스 200 응답 확인은 했지만, 픽셀 단위 레이아웃/겹침 문제는
  사용자가 실제 화면에서 확인해주셔야 합니다.
- "지구형" 레이아웃은 사용자가 "별로면 빼도 된다"고 한 실험적 기능이라 완성도보다 속도를 우선했습니다.
